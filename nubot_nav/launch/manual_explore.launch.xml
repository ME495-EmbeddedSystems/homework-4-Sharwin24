<launch>

  <!-- Start the nubot simulation -->
  <include file="$(find-pkg-share nubot_nav)/simulate.launch.xml">
    <arg name="use_rviz" value="true" />
    <!-- <arg name="world" value="none" /> -->
  </include>

  <!-- Start the slam_toolbox in async mode -->
  <node name="slam_toolbox" pkg="slam_toolbox" exec="async_slam_toolbox_node" output="screen">
    <param name="use_sim_time" value="true"/>
    <!-- <param name="slam_params_file" value="$(find slam_toolbox)/param/slam_params.yaml"/> -->
  </node>

  <!-- Start the nav2 stack -->
  <!-- <node name="nav2" pkg="nav2_bringup" exec="nav2_bringup" output="screen">
    <param name="use_sim_time" value="true"/>
    <param name="params_file" value="$(find nubot_nav)/config/nav2_params.yaml"/>
    <param name="autostart" value="true"/>
  </node> -->

  <!-- Start rviz -->
  <!-- <node name="rviz" pkg="rviz2" type="rviz2" args="-d $(find nubot_nav)/config/nav2_default_view.rviz"/> -->

</launch>
